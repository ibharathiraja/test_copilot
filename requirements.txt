# requirements.txt
Flask==2.3.0
Werkzeug==2.3.0

# tasks.json (initial data file)
[]

# README.md content:
# Task Manager Flask App

A simple task management application built with Flask.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python app.py`
3. Open browser: `http://localhost:5000`

## Known Issues
This project contains intentional bugs for testing GitHub Copilot's issue fixing capabilities.

# .gitignore
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
env/
*.log
.DS_Store
tasks.json
